import styled from 'styled-components';

export const Section = styled.section`
    width: 400px;
    /* tiramos o height: 300px; (padrão) para que o componente 'userNumbers' entrasse no espaço branco
    da div <UserContainer></UserContainer> */
    background: rgba(255,255,255,.95);
    border-radius: 10px;
    border-bottom: 10px solid #FF7A00;
    margin-top: 15px;
`